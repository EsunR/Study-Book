import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <React.StrictMode>
    <div onClick={() => {}}>
      <h3>hello, react</h3>
    </div>
  </React.StrictMode>,
  document.getElementById("root")
);
