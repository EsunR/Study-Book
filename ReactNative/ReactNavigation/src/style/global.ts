import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  flexCenter: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  fullWH: {
    width: '100%',
    height: '100%',
  },
});
